# `dummy_moveit_ws-main` 复现路线

这份工作区先按“两阶段”使用：

1. 先复现 `MoveIt 2 + ros2_control` 仿真链路
2. 再切到真实机械臂

## 1. 工作区结构

- `dummy-ros2_description`
  机器人 URDF/Xacro 和网格模型
- `dummy_moveit_config`
  MoveIt 配置、控制器配置、启动文件
- `dummy_hardware`
  `ros2_control` 真机硬件插件，负责串口对接主控固件
- `dummy_controller`
  上游作者原来的 Python 真机桥，主要用于参考关节映射
- `dummy_server`
  `pymoveit2` Python 调用层

## 2. 当前复现顺序

### 第一步：先跑假硬件

这一步的目标不是控制真实机械臂，而是确认：

- URDF 正常
- TF 正常
- `ros2_control` 控制器能起来
- MoveIt 规划和执行链路正常

当前工作区已经改成：

- [dummy-ros2.urdf.xacro](./dummy_moveit_config/config/dummy-ros2.urdf.xacro)
  默认 `use_fake_hardware=true`
- [dummy-ros2.ros2_control.xacro](./dummy_moveit_config/config/dummy-ros2.ros2_control.xacro)
  `fake hardware` 走 `mock_components/GenericSystem`
  真机走 `dummy_hardware/DummyRobotSystem`

推荐命令：

```bash
cd /home/peter/桌面/线轨夹爪资料/其他资料/dummy_moveit_ws-main
colcon build --symlink-install
source install/setup.bash
ros2 launch dummy_moveit_config demo.launch.py
```

预期结果：

- RViz 能打开
- 能看到机械臂模型
- `MotionPlanning` 面板可以规划
- 规划后可以执行到 fake hardware

### 第二步：再切真实机械臂

真机入口现在用：

```bash
ros2 launch dummy_moveit_config demo_custom.launch.py serial_port:=/dev/ttyACM0
```

这一路会走：

- [demo_custom.launch.py](./dummy_moveit_config/launch/demo_custom.launch.py)
- [dummy_robot_system.cpp](./dummy_hardware/src/dummy_robot_system.cpp)

当前真机桥已具备：

- 激活时发 `!START`
- 激活时发 `#CMDMODE 2`
- `read()` 查询 `#GETJPOS`
- `write()` 发送 `>j1,j2,j3,j4,j5,j6`

## 3. 真机接入时必须继续补的内容

### 3.1 关节映射

最先要确认的是 ROS 角度和真机角度是否一致：

- 方向是否相反
- 零位是否偏移
- `Joint3` 是否需要额外 `+90deg`
- `Joint5/Joint6` 是否需要取反

上游作者的旧真机桥里已经有这层映射，可作为参考：

- [dummy_arm_controller.py](./dummy_controller/dummy_controller/dummy_arm_controller.py)

里面的关键量：

- `rad_volumn_diff = [0, 0, 1.57079, 0, 0, 0]`
- `rad_direct_diff = [1, 1, 1, 1, -1, -1]`

这层映射最终应该挪到：

- [dummy_robot_system.cpp](./dummy_hardware/src/dummy_robot_system.cpp)

### 3.2 夹爪

当前工作区还没有把夹爪完整接进：

- URDF
- SRDF
- `ros2_control`
- MoveIt group

所以当前先只按 6 轴机械臂复现。

夹爪接入顺序应为：

1. URDF 增加 `gripper_joint`
2. `ros2_control` 增加夹爪 command/state interface
3. 确认固件支持夹爪串口命令
4. SRDF 增加 `gripper` group 和 `end effector`

### 3.3 障碍物与抓取

当前先不做视觉。

先补静态场景：

- 桌面
- 支架
- 固定夹具

等 `MoveIt + 真机执行` 跑通后，再做：

- 目标识别
- 抓取位姿生成
- 动态障碍物更新

## 4. 现在这份工作区最重要的文件

- [dummy_moveit_config/config/dummy-ros2.ros2_control.xacro](./dummy_moveit_config/config/dummy-ros2.ros2_control.xacro)
- [dummy_moveit_config/config/dummy-ros2.urdf.xacro](./dummy_moveit_config/config/dummy-ros2.urdf.xacro)
- [dummy_moveit_config/launch/demo.launch.py](./dummy_moveit_config/launch/demo.launch.py)
- [dummy_moveit_config/launch/demo_custom.launch.py](./dummy_moveit_config/launch/demo_custom.launch.py)
- [dummy_hardware/src/dummy_robot_system.cpp](./dummy_hardware/src/dummy_robot_system.cpp)
- [dummy_controller/dummy_controller/dummy_arm_controller.py](./dummy_controller/dummy_controller/dummy_arm_controller.py)

## 5. 当前边界

- 这份工作区目前还没有在本机完成 ROS 运行验证
- `dummy_hardware` 现在只接了 6 个关节
- 真机关节映射还没有最终按你的机械臂实测校准
- 夹爪还没有并入 MoveIt

所以当前最稳的推进顺序是：

1. 先跑 `demo.launch.py`
2. 再验证 Linux 串口能和固件通信
3. 再跑 `demo_custom.launch.py`
4. 再补关节映射
5. 最后接夹爪和抓取
